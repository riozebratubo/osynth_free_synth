<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Substituted by CMake from the target's MACOSX_BUNDLE_GUI_IDENTIFIER,
         i.e. APP_ID: org.osynth.osyntho for the controller build,
         org.osynth.osyntho.standalone for the embedded one. It is also the
         single-instance lock name, which is what lets the two run at once, so
         do not hard-code it back: a fixed value here would make the second
         app launched exit into the first one's window. (No double hyphens in
         these comments; XML reads one as the start of the closing bracket.)
         deploy_macos_multi.sh checks the built bundle against the variant it
         detects in the build tree. -->
    <key>CFBundleIdentifier</key>
    <string>@MACOSX_BUNDLE_GUI_IDENTIFIER@</string>
    <key>CFBundleName</key>
    <string>@MACOSX_BUNDLE_BUNDLE_NAME@</string>
    <key>CFBundleDisplayName</key>
    <string>@MACOSX_BUNDLE_BUNDLE_NAME@</string>
    <key>CFBundleShortVersionString</key>
    <string>@MACOSX_BUNDLE_SHORT_VERSION_STRING@</string>
    <key>CFBundleVersion</key>
    <string>@MACOSX_BUNDLE_BUNDLE_VERSION@</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleSignature</key>
    <string>????</string>
    <key>CFBundleExecutable</key>
    <string>@MACOSX_BUNDLE_EXECUTABLE_NAME@</string>
    <key>NSHighResolutionCapable</key>
    <true/>
    <!-- Required for CoreBluetooth: without a usage string macOS terminates the
         process the first time QBluetoothDeviceDiscoveryAgent starts a scan.
         No location keys here — unlike iOS/Android, macOS does not gate BLE
         scanning behind location permission. No camera/photo keys either: the
         only QtMultimedia user (src/camerapreviewitem.cpp) is not in the build. -->
    <key>NSBluetoothAlwaysUsageDescription</key>
    <string>Osyntho uses Bluetooth to connect to your Osynth synthesizer.</string>
    <key>NSBluetoothPeripheralUsageDescription</key>
    <string>Osyntho uses Bluetooth to connect to your Osynth synthesizer.</string>
    <!-- Required, not optional: App::startEmbeddedEngine() requests the
         microphone permission on the standalone build, and macOS terminates a
         process that requests a permission with no usage string here. The
         controller build never asks, and an unused string costs it nothing. -->
    <key>NSMicrophoneUsageDescription</key>
    <string>Osyntho uses the audio input as a sound source: the line input, the vocoder and the granular engine all read from it.</string>
    <!-- Matches CMAKE_OSX_DEPLOYMENT_TARGET in CMakeLists.txt. Declaring an
         older floor here than the toolchain built for lets the app launch on
         systems where it then dies on a missing symbol. -->
    <key>LSMinimumSystemVersion</key>
    <string>14.0</string>
</dict>
</plist>
